var state = {
  active_keys : {},
};

var Synthesizer = function() {
  // dom
  // event listeners
  // inialization code  
};

var Keyboard  = function(state){
  // dom
  this.el = document.getElementById('keyboard');

  // model
  this.keys = [];
  for (var i = 0;)

  // event listeners
  this.el.addEventListener('touchstart', function(e){
    state.active_keys[]; 
  });

  // inialization code  
};

var Keys = function(){
  // dom
  // event listeners
  // inialization code  

};
